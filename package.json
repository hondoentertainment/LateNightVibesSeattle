{
  "name": "late-night-vibes-seattle",
  "version": "1.0.0",
  "private": true,
  "description": "Discover Seattle nightlife venues open after 9 PM",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint *.js lib/*.js",
    "lint:fix": "eslint --fix *.js lib/*.js"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "eslint": "^9.0.0"
  }
}
